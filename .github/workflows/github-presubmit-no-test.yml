name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'terraform/**'
      - 'clients/go/**'
      - 'clients/java-logger/**'
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'terraform/**'
      - 'clients/go/**'
      - 'clients/java-logger/**'

# If a new commit comes in, cancel the current action run.
concurrency:
  group: '${{github.workflow}}-${{ github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Files changed do not require test."'
